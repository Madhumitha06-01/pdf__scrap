# pdf__scrap

## INSTALLATION
To install `pypdf2` from PyPI, run the following command:

```bash
pip install PyPDF2
pip install pypdf2image
pip install pandas
pip install openpyxl
pip install pymupdf

